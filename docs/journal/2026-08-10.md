# 2026-08-10

## Summary
Grosse journée en trois actes, pilotée par l'usage réel. (1) **Optimisation totale** : trois audits parallèles (réactivité Svelte, rendu/GPU, poids/démarrage) puis trois lots de fixes — dist **10,4 Mo → 4,5 Mo (−57 %)**, fonts au boot 5,3 Mo → 285 Ko (subset Material Symbols 5,1 Mo → 12,5 Ko avec outillage `scripts/` + test garde-fou), entrée JS 549 → 268 Ko, fenêtre native sans flash blanc (`visible:false` + `show()` post-montage, filet Rust 4 s), copilote/Paramètres chargés paresseusement, chemins chauds assainis (scroll-spy mémoïsé, tableaux sans re-parcours par flèche, collateurs Intl, débounce recherche). Au passage : **bug Geist corrigé** (`'Geist'` → `'Geist Sans'` — l'UI tournait en police système depuis le début) et **artefact du panneau copilote définitivement réglé** (plus d'opacité ni de translateX : rideau pur en largeur, comme la sidebar). (2) **Citations ancrées façon NotebookLM** (étude du dépôt open-notebook) : extraits numérotés par l'app, puces `[n]` cliquables validées/assainies, saut au passage exact avec flash dans l'éditeur, **pages PDF : défilement + surlignage ambré des lignes citées** (correspondance page↔offset conservée à l'extraction, rectangles des items de texte en % de page), pied « Passages consultés/cités » déterministe, budget de contexte par fournisseur (12k local / **240k cloud** — un PDF de 5 pages était tronqué en OpenAI). (3) **Sauver une réponse en note .md** (provenance capturée à la génération, marqueurs retirés, écriture `createNew` atomique) + **réécritures structurelles de sélection** (« En liste à puces », « En cases à cocher ») sur l'infra d'aperçu diff 16.2 — via /plan → /critic (4 Major intégrés) → /epct → revue de code (2 Major corrigés : extension `.pdf` piégée dans un nom de note, formule de hauteur du menu). Validations : `npm run check` 0 erreur, **367 tests** (330 → 367), vérifs Playwright deux thèmes, tests natifs utilisateur (installateur recompilé 2×, citations validées sur PDF réel en OpenAI).

## Added
- `scripts/` — outillage du subset d'icônes : `subset-icons.mjs` (API Google Fonts `icon_names=`, réseau à la régénération seulement), `icon-names.mjs` (extraction partagée script/test), table codepoints, README.
- `src/assets/material-symbols-rounded.subset.woff2` + `material-symbols-manifest.json` — police d'icônes 12,5 Ko (83 icônes) committée.
- `src/lib/icons.test.ts` — garde-fou : icône ajoutée hors subset = test rouge.
- `src/lib/citations.ts` + tests — annotation des `[n]` en puces (post-DOMPurify, zones de code préservées), `citedNumbers`, `locateOffset`/`locatePassage` (sondes dégressives), `stripCitationMarkers`.
- `src/lib/notes.ts` + tests — nommage (`Doku-San — <question>.md`, extension forcée `.md`, suffixes de conflit) et contenu (provenance, passages consultés) des notes sauvées.
- `docs/plans/notes-ia-et-reecritures-structurelles.md` — plan /plan + critique intégrée.

## Modified
- `src/lib/editor/{editor,live-preview,rephrase-preview}.ts` — extensions CM6 en singletons, court-circuit `isActive`, tableField sans rebuild sur simple curseur, widget de reformulation muté par token (plus de rebuild DOM).
- `src/lib/{explorer,search,stores.svelte}.ts` — collateurs Intl réutilisés, tri mémoïsé par dossier, `lineAt` incrémental, mémos `docHeadings`/`isDirty`, dédup saveSettings, `openCopilot` (montage paresseux séquencé), `pendingPdfReveal`.
- `src/components/Sidebar.svelte` — Map onglets-par-chemin, débounce recherche 150 ms, `contain` sur `.inner`/`.panel-body`.
- `src/components/DocumentView.svelte` — scroll-spy throttlé rAF, sélection sans copies géantes, menu de sélection : 6 verbes (+ puces, + cases à cocher) et hauteur mesurée (chrome + tiroir).
- `src/components/CopilotPanel.svelte` — rideau largeur seule (artefact réglé), `contain` sur `.cop-scroll`, autoscroll rAF, puces `[n]` + pied numéroté sautant au passage, bouton « Sauver en note » (feedback succès/échec), badge contexte au budget du fournisseur.
- `src/components/PdfView.svelte` — wrappers de page positionnés, révélation de page citée (scroll + halo) et surlignage ambré des rectangles du passage (fondu 4 s), `prefers-reduced-motion` respecté.
- `src/lib/{copilot.svelte,copilot-service}.ts` — extraits numérotés + consigne de citation (3 modes), `buildCitedDocChatMessages` (document complet en extraits, les 2 fournisseurs), `MAX_DOC_CHARS_CLOUD` 240k, `sourceLabel` capturé à la génération, `jumpToCitation` (md + PDF), `saveMessageAsNote`, modes `bullets`/`tasks` (règle de mise en forme conditionnelle, sélection étendue aux lignes).
- `src/lib/{pdf,pdf-text}.ts` — `pageStarts` (page↔offset, numéros réels), `pageForOffset`, `assemblePageItems`, `matchPassageRange`, `getCitedRects` (fractions de page, rotation gérée).
- `src/lib/tauri.ts` — `createFileWithContent` (`createNew` atomique).
- `src/{main.ts,app.css}` — imports de fonts assainis (subset icônes, Source Serif latin seul, Geist 300 retiré, famille `'Geist Sans'` corrigée), reflet du chrome seuillé sans transition racine, shimmer composité.
- `vite.config.ts` — `target: esnext`, purge des `.woff` doublons, sans polyfill modulepreload.
- `src-tauri/{tauri.conf.json,src/main.rs}` — fenêtre invisible jusqu'au premier rendu + filet de sécurité 4 s.
- `AGENTS.md` — commande `subset:icons` + dossier `scripts/` documentés.

## Fixed
- Artefact visuel à l'ouverture/fermeture du panneau copilote (bande sombre) — deux causes successives éliminées (fondu d'opacité, puis translateX) ; règle finale : un panneau latéral s'anime en largeur seulement.
- L'UI entière s'affichait en police système : `--font-sans: 'Geist'` ne correspondait à aucune `@font-face` (`'Geist Sans'`).
- Un PDF > 12k caractères était tronqué en OpenAI (plafond local appliqué au cloud) → budget par fournisseur.
- Nom de note dérivé d'une question finissant par `.pdf`/`.html` → extension `.md` forcée (revue de code).

## Discoveries
- Le subset local (hb-subset) de Material Symbols casse les ligatures de certains noms (`search`, `keep`, `check`… — substitutions en chaîne avec glyphes intermédiaires) : sans fermeture GSUB elles sont élaguées, avec fermeture les 3 600 glyphes reviennent (356 Ko). Le subsetter officiel Google (`css2?icon_names=`) est la voie fiable — documenté dans `scripts/subset-icons.mjs`.
- Fenêtre Playwright occluse : rAF et transitions gelés (~1 fps) — échantillonner en boucles rAF DANS une seule évaluation ; des animations mises en pause via `getAnimations()` persistent et bloquent les transitions suivantes de l'élément.
- Une propriété CSS personnalisée héritée animée sur `:root` invalide le style de tout le document et repeint le fond plein écran à chaque frame (reflet du chrome) — seuiller les écritures et bannir la transition.
- Open Notebook (étude) : ne jamais faire recopier des IDs opaques à un LLM — numéroter côté app, le modèle ne recopie qu'un chiffre ; leur clic ouvre une modale, Doku va plus loin (passage exact surligné).

## Decisions
- Citations ancrées : validation déterministe côté app (numéro hors liste → retiré ; refus honnête → pas de sources) ; petit document = document complet découpé en extraits numérotés (aucun embedding, marche aussi en cloud) ; pied « Passages cités » filtré aux extraits réellement cités dans ce mode.
- PDF : précision à la page + surlignage des rectangles d'items — jamais de faux surlignage (passage introuvable → halo de page ; scanné → ouverture simple).
- Provenance d'une note sauvée : capturée à la fin de la génération (`sourceLabel`), jamais résolue au clic ; sans certitude → pas de « d'après ».
- Budgets de contexte par fournisseur : 12k local (index éphémère au-delà) / 240k cloud (troncature signalée au-delà) — badge et comportement sur le même seuil.
- Non retenu d'Open Notebook : flux « Ask » multi-requêtes (latence ×N et décomposition douteuse sur 1.5b), podcast/STT, notebooks/API (le système de fichiers joue ce rôle). Retenu pour plus tard : transformations personnalisées, contrôle du contexte (→ /sprint plan).

## Commits
| Hash | Message |
|---|---|
| `0b760e8` | perf: runtime, rendu et demarrage optimises |
| `578d9b9` | feat: citations ancrees, slide copilote corrige |
| `ea73464` | feat: saut et surlignage du passage cite en pdf |
| `b7b8dd4` | feat: sauver en note + reecritures structurelles |
