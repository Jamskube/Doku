# Journal — 2026-08-12

## Summary
Doku dispose maintenant d'un installateur Windows x64 reproductible, testé localement et sur GitHub Actions. La régression de l'animation des points pendant le streaming a également été corrigée. Le bouton `+` du chat ajoute désormais un contexte éphémère borné, et la mémoire cloud durable est isolée par document par défaut avec partage de dossier uniquement sur choix explicite. Vérifications exécutées : `npm run check`, `npm test` (452 tests), `npm run build`, `npm run tauri build -- --bundles nsis`, `npm run build:installer:x64`, contrôle PE x64, smoke HTTP Ollama 0.32.0 et vérification visuelle navigateur en thèmes clair/sombre.

Le chrome natif utilise désormais le vrai Mica Windows 11 uniquement en mode sombre, appliqué avant le premier affichage de la fenêtre ; le mode clair conserve le matériau CSS opaque. La palette sombre hors Mica est recentrée sur un graphite neutre `#181818`. Les installateurs NSIS ARM64 et x64 ont été reconstruits et leurs architectures PE vérifiées. Commandes exécutées pour cette passe : `npm run check`, `npm test -- --run`, `npm run build`, `cargo check`, `git diff --check`, `npm run build:installer:arm64` et `npm run build:installer:x64`.

## Added
- `/.github/workflows/build-windows-x64.yml` — construit, installe, smoke-teste et publie automatiquement l'installateur NSIS x64.
- `/scripts/prepare-ollama-sidecar.mjs` — prépare les sidecars Ollama CPU ARM64 ou x64 depuis les archives officielles épinglées.
- `/src-tauri/resources/` — embarque la licence Ollama et documente les ressources distribuées.
- `/DESIGN.md` et `/.impeccable/design.json` — formalisent le système visuel extrait de l'interface Doku.
- `/.impeccable/critique/2026-08-11T11-09-17Z__src-components-copilotpanel-svelte.md` — conserve l'audit indépendant du panneau copilote.
- `/docs/plans/ajouter-contexte-chat.md` — plan critiqué puis corrigé pour les sources éphémères du composer.
- `/docs/adr/0019-memoire-travail-cloud-automatisee.md` — décide une mémoire Markdown locale, automatisée pour les fournisseurs cloud et contrôlable par l'utilisateur.
- `/src/lib/copilot-context.ts` et `/src/lib/copilot-context.test.ts` — bornes, identité, déduplication et budget partagé des sources ajoutées au chat.
- `/src/lib/copilot-memory.ts`, `/src/lib/copilot-memory.svelte.ts` et leurs tests — extraction, rappel sélectif, stockage, édition, oubli et annulation de la mémoire cloud.

## Modified
- `/src-tauri/Cargo.toml` et `/src-tauri/Cargo.lock` — ajoutent la dépendance de vibrance Windows nécessaire au Mica.
- `/src-tauri/src/main.rs` — active ou retire le Mica Windows selon le thème et conserve le filet d'affichage de la fenêtre.
- `/src/App.svelte`, `/src/app.css`, `/src/main.ts` et `/src/lib/stores.svelte.ts` — rendent uniquement le chrome Mica transparent, garantissent l'activation avant `show()` et assombrissent les surfaces fonctionnelles autour de `#181818`.
- `/package.json` — ajoute les commandes de préparation Ollama et de compilation des installateurs ARM64/x64.
- `/AGENTS.md` et `/README.md` — documentent le support Windows ARM64/x64 et les nouvelles commandes.
- `/src-tauri/binaries/README.md` — décrit le flux multiarchitecture et les contrôles de cohérence.
- `/src-tauri/tauri.conf.json` — embarque les licences et autorise la transparence native nécessaire au Mica.
- `/src/components/CopilotPanel.svelte` — finalise le polish Impeccable, ajoute le menu de contexte, la vue mémoire et le sélecteur explicite « Cette note / dossier partagé ».
- `/src/lib/copilot.svelte.ts`, `/src/lib/copilot-service.ts` et leurs tests — injectent les contextes bornés et les souvenirs rappelés sans modifier Ollama.
- `/src/lib/tauri.ts` et `/src-tauri/capabilities/default.json` — ajoutent les primitives locales nécessaires aux fichiers de contexte, au stockage mémoire AppData et à la synchronisation du matériau système.
- `/docs/design/w2-copilot/README.md`, `/docs/adr/README.md`, `/src/README.md` et `/docs/plans/_latest.md` — documentent le nouveau comportement, le rendu Mica et ses décisions.

## Fixed
- `/src/components/CopilotPanel.svelte` — préserve le mouvement des points pendant le streaming après minification CSS.
- `/.github/workflows/build-windows-x64.yml` — smoke-teste l'installation dans le chemin NSIS réel sous `%LOCALAPPDATA%\Doku`.
- `/src/main.ts` et `/src/lib/stores.svelte.ts` — restaurent l'ordre Mica puis affichage qui empêche le DWM de figer un fond opaque au démarrage.

## Discoveries
- Le payload Ollama x64 CPU charge `llama-server.exe` et génère réellement avec Qwen sous Windows x64.
- L'installateur NSIS Tauri courant installe silencieusement dans `%LOCALAPPDATA%\Doku` ; le dossier `/D=` attendu n'était pas utilisé par le smoke test initial.
- Préparer le sidecar x64 remplace le contenu de staging `src-tauri/binaries/lib/ollama/` par son architecture ; relancer `npm run prepare:ollama:arm64` avant un prochain build ARM64, et inversement.
- Une mémoire inférée depuis l'explorateur ou le dossier parent transforme trop facilement `Desktop` en « projet » : la portée sûre est le document, le dossier restant un partage volontaire et visible.
- Sous Tauri/Windows 11, appliquer Mica après le premier `show()` peut laisser le DWM composer un fond opaque ; l'effet doit être demandé pendant que la fenêtre est encore cachée.
- `window-vibrancy` expose `clear_mica`, ce qui permet une vraie désactivation dynamique en thème clair sans simuler un cache CSS par-dessus l'effet système.

## Decisions
- Le paquet x64 embarque uniquement le moteur Ollama CPU nécessaire afin de garder l'installateur à environ 20 Mo.
- Le build x64 est reproductible par commande locale et par workflow GitHub manuel ou déclenché sur tag `v*`.
- Les ajouts du bouton `+` restent éphémères, retirables et purgés avec une nouvelle conversation ; les chemins locaux ne sont jamais envoyés au fournisseur.
- La mémoire durable fonctionne uniquement avec OpenAI et MiniMax, reste locale en Markdown et s'attache par défaut à la note enregistrée ; un dossier n'est partagé qu'après un geste explicite.
- Mica reste réservé au thème sombre ; le thème clair utilise le matériau CSS opaque, et les surfaces sombres hors chrome suivent une rampe neutre centrée sur `#181818`.

## Commits
| Commit | Description |
|---|---|
| `15a8652` | `fix: preserve streaming dot motion` |
| `09b08d3` | `feat: add Windows x64 installer` |
| `e11ab2c` | `fix: repair x64 installer smoke test` |
| `52a43e8` | `update: polish copilot interactions` |
| `1605bba` | `docs: update session journals` |
| `27e8399` | `feat: add chat context memory` |
| `71d3367` | `docs: update session handoff` |
