# 2026-07-27

## Summary
Sprint 16 planifié, exécuté et clos **le jour même** : validation native de 19.2 (modale Paramètres, purges éprouvées) et 19.3 (logo → À propos) actée par l'utilisateur, puis **v2.2.0 scellée** (19.4 : bump partout + installateur NSIS ARM64 + smoke app installée). **Epic 19 soldé — plus aucun bouton muet.** En parallèle, sur retour direct de l'utilisateur (« pas assez stylisé »), l'installateur a été **entièrement habillé en D.A. Doku** (design hors sprint, 2 itérations) : BMPs sidebar/header rendus en navigateur avec les vraies fontes Geist, puis template NSIS custom (fonds crème/encre sur toutes les pages, journal « papier », BrandingText, français). Rétro S16 faite. Ledger : **69 features, 68 PASS** (17.2 annulée par conception). Backlog vide — cap sur l'usage réel.

## Added
- `docs/sprints/sprint-16.md` — sprint de clôture (3 stories S), clos J+0
- `docs/sprints/retro-sprint-16.md` — rétro 3/3, 3 leçons (périphérie D.A., fork de template = dette datée, encodage PowerShell)
- `src-tauri/installer/sidebar.bmp` + `header.bmp` — visuels NSIS 164×314 / 150×57 (crème AIR, mark D, Geist), rendus à 2× puis downscalés en BMP 24-bit
- `src-tauri/installer/installer.nsi` — template NSIS custom, base officielle **tauri-cli 2.11.4**, blocs marqués `; DOKU:`
- `docs/design/installer-nsis/` — générateur des visuels (`generate-art.mjs`) + README (recette complète + dette de re-diff à l'upgrade CLI)
- READMEs structurels (tidy) : `docs/planning/`, `docs/design/`, `docs/sprints/` (règle ledger), `docs/journal/`, `src-tauri/installer/` (avertissement re-diff)

## Modified
- `docs/sprints/ledger.json` — sprint → 16 ; +19.4 ; 19.2/19.3/19.4 flippées PASS (validées utilisateur)
- `package.json` + lock, `src-tauri/tauri.conf.json`, `src-tauri/Cargo.toml` + lock — version **2.2.0** (la carte À propos lit `package.json`)
- `src-tauri/tauri.conf.json` — section `bundle.windows.nsis` : template custom, installerIcon, header/sidebar, langue **française**
- `README.md` racine — remis au réel (tidy) : il disait encore « stack pas choisie » → v2.2.0, stack, commandes, structure
- `docs/README.md` — + lignes `design/` et `sprints/` ; `AGENTS.md` — + `npm test`, + `public/`, `spike/` reformulé, **+ 3 memories** (périphérie D.A. / template NSIS figé / encodage PowerShell)

## Fixed
- `AGENTS.md` + `docs/sprints/retro-sprint-5.md` — **2 octets NUL littéraux chacun** remplacés par la notation `\u0000` : Doku (usage réel !) refusait d'ouvrir `AGENTS.md` (« fichier binaire, impossible à afficher »). La détection (`encoding.ts`, NUL ⇒ binaire) a fonctionné comme spécifié — le fichier était fautif, pas l'app. Ironie : les NUL vivaient DANS la gotcha de la rétro S5 qui met en garde contre les caractères de contrôle littéraux matérialisés par l'outil Write — et la correction elle-même a réinséré un NUL dans ce journal (attrapé au diff git « Bin », assaini). Symptôme fiable : git compte le fichier en binaire dans `--stat`.

## Discoveries
- L'installateur NSIS par défaut de Tauri est **en anglais** et brut — jamais regardé en 15 sprints : la D.A. s'arrêtait au bord de la fenêtre de l'app.
- `bundle.windows.nsis.template` permet de remplacer le template entier → fonds/textes/désinstalleur habillables (`MUI_BGCOLOR`, `SetCtlColors`, `MUI_INSTFILESPAGE_COLORS`, `BrandingText` avec échappements `${U+XXXX}`). Limite dure : boutons/checkbox/progressbar restent Win32 natifs.
- Playwright MCP : `file://` bloqué (servir en localhost) ; screenshots seulement sous `G:\Doku\.playwright-mcp`.
- PowerShell 5.1, deux pièges d'encodage la même session : `Get-Content`/`-replace`/`Set-Content` mojibake les accents (lecture ANSI) — `Cargo.toml` corrompu puis restauré via git ; `Out-File -Encoding utf8` met un BOM qui atterrit dans le titre du commit (`-F`). Parade : outil Edit/node + `WriteAllText(UTF8 sans BOM)`.

## Decisions
- **Version 2.2.0** gravée (utilisateur) — fini le `0.1.0` qui mentait à la carte À propos.
- Habillage installateur = **design hors sprint** (préférence utilisateur) : aucune entrée ledger, itéré sur retour direct.
- Pas de numéro de version dans les bitmaps de l'installateur (il mentirait au bump suivant).
- Rétro S16 : action High = **utiliser Doku au quotidien** (reconduite S15), le prochain epic doit émerger de l'usage.

## Commits
| Hash | Message |
|---|---|
| `4636ac1` | docs: sprint 16 planifie + 19.2/19.3 validees en natif (ledger flippe) |
| `76f8da2` | chore: version 2.2.0 (19.4) - package.json, tauri.conf.json, Cargo.toml |
| `844d6dd` | feat: habillage NSIS de l'installateur en D.A. Doku (design hors sprint) |
| `a4c08cc` | feat: template NSIS custom - installateur entierement en D.A. Doku |
| `3628114` | docs: 19.4 validee - sprint 16 3/3, v2.2.0 scellee (reste la retro) |
| `aebf19d` | docs: retro sprint 16 - 3/3, sprint clos (v2.2.0, Epic 19 solde) |
| `0862e44` | docs: wrap session - sprint 16 clos, v2.2.0, installateur D.A., 3 memories |
| `5ed45ce` | fix(docs): NUL litteraux assainis (AGENTS.md, retro-sprint-5) |
| `e5096a6` | docs: tidy - README racine remis au reel (v2.2.0), 5 READMEs crees |
