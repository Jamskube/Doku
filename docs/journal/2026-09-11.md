# 2026-09-11 — Revue croisée du travail des 6 et 9 septembre, un Critical corrigé

## Summary

Le mainteneur demande une revue de tout le non-commité : mon travail du 6 (composeur, palette « / », marqueurs `[n]`, 3.5.0) et celui d'un autre agent le 9 (images collées, Markdown portable, 3.5.1). Deux relecteurs indépendants, un reviewer et un critique, lancés en parallèle sur le même diff. **Ils ont convergé sur cinq défauts** sans se parler, dont un **Critical dans mon propre code** : le retrait des marqueurs `[n]`, appliqué à la chaîne HTML, réécrivait les feuilles de style de chaque document généré. Tout est corrigé et verrouillé par des tests. Aucun commit : 32 fichiers attendent.

## Fixed

- **Critical — CSS corrompu dans tout document généré.** `annotateCitations(html, 0)` sur la chaîne : son nettoyage de l'espace orpheline (`/ +([.,])/`) transformait `.card .title` en `.card.title` et `margin: 0 .5em` en `0.5em`, ainsi que les tracés SVG. Reproduit en `node -e`. Désormais sur les nœuds texte du DOM, hors `style/script/code/pre/svg/textarea`. `generated-document.ts`, `citations.ts::stripMarkersInText`.
- **Major — les images collées restaient en placeholder après le premier enregistrement.** Le correctif du 9 reconfigurait la facette, mais le `ViewPlugin` ne rebâtit ses décorations que sur `docChanged/selectionSet/viewportChanged` ; ajout de la comparaison `startState.facet(docDirFacet) !== state.facet(docDirFacet)`. `live-preview.ts`.
- **Major — la jauge de verbosité était inatteignable au clavier** : `onAddMenuKeydown` interceptait les flèches pour tous les enfants du menu. Le `<input type=range>` garde ses flèches et prend le focus à l'ouverture de sa section. `CopilotPanel.svelte`.
- **Major — les index RAG persistés gardaient leurs chunks base64** : `RAG_META_VERSION` 1 → 2, reconstruction. `rag.ts`.
- **Major — 240 k octets d'image partaient au modèle** : `truncateContextItem` coupait AVANT le retrait des images ; une data-URL tronquée n'a plus sa parenthèse et échappe à la regex. Retrait avant coupe. `copilot-context.ts`.
- **Major — le badge Contexte annonçait une lecture partielle d'un document envoyé entier** (comptait les octets d'images). `textLengthOf` partagé. `CopilotPanel.svelte`.
- **Sécurité — le Markdown portable pouvait embarquer n'importe quel fichier du disque** (`![x](../../.ssh/id_rsa)`, chemin absolu). Seules les images SOUS le dossier du document sont intégrées ; `isInsideDir` canonise `\`/`/` et `..`. `portable-markdown.ts`.
- **Reformulation d'une sélection contenant une image** : refusée avec bannière — on ne peut ni l'envoyer ni la retirer sans la perdre. `copilot.svelte.ts`.
- **`omitEmbeddedImageData` ignorait `<img src="data:…">`** (onglet HTML) — étendu. `images.ts`.
- **Un Markdown portable de 65 Mo restait en prévisualisation** : plafond brut `HEAVY_RAW_CEILING` 8 Mo en plus du seuil texte. `stores.svelte.ts::isHeavyContent`.
- **`/web météo` coupait la recherche si elle était active** : avec une suite, la commande ACTIVE ; seule, elle bascule. `/` puis Entrée n'arme plus la première ligne à l'aveugle. Les commandes de contexte attendent la fin d'un chargement en cours. L'erreur de contexte se lit aussi sous le champ de question. `CopilotPanel.svelte`.
- `portableMarkdownName` idempotent ; `verbosityLabel` mort retiré ; `packaging/arch/PKGBUILD` 3.3.0 → 3.5.1.

## Discoveries

- **Un nettoyage textuel de ponctuation appliqué à une chaîne HTML réécrit le CSS et le SVG.** Le remède est structurel : opérer sur les nœuds texte d'un DOM parsé, jamais sur la sérialisation. → record machine.
- **Un `<dialog>` perd son centrage sous un reset universel `* { margin: 0 }`** : le navigateur le centre par `margin: auto`, que le reset écrase ; il faut le rétablir explicitement, plus `max-width/max-height: none` contre les bornes de l'agent utilisateur. → record machine.
- **Reconfigurer un `Compartment` CM6 ne rebâtit pas un `ViewPlugin`** qui ne surveille que `docChanged/selectionSet/viewportChanged` : comparer la facette entre `startState` et `state`. → record projet.
- **Retirer un blob AVANT de tronquer** : coupé au milieu, le blob n'est plus reconnaissable et passe entier. → record projet.
- **Changer un découpeur sans incrémenter la version de l'index persisté** laisse les anciens chunks en place, le hash de fichier ne bougeant pas. → record projet.
- **Deux relecteurs à contextes séparés qui convergent** sur cinq points sans se parler : signal plus fort que dix tours séquentiels ; chaque point a été vérifié à la source avant correction. *(déjà porté par la règle du swarm — journal)*
- **Des installateurs produits sans commit ne correspondent à rien** : 3.5.0 et 3.5.1 sont deux binaires sans hash, `Cargo.lock` modifié hors commit. Un rapport « en 3.5.1 » est inattribuable. *(journal — c'est au mainteneur de commiter et tagger)*
- **Git Bash place GNU `tar` devant celui de Windows** : un script npm qui extrait un ZIP par `tar` échoue depuis Bash et passe depuis PowerShell. *(non retenu, cap)*

## Decisions

- **Les Major des deux relecteurs sont tous corrigés**, y compris ceux du travail de l'autre agent : la revue validait l'ensemble, pas un auteur.
- **Ni commit ni tag** : c'est le geste du mainteneur. Les installateurs 3.5.1 du 9 ne contiennent pas les correctifs du jour et ne doivent pas être distribués.

| Vérification | Résultat |
|---|---|
| `npm run check` | 0 erreur, 1131 fichiers |
| `npx vitest run` | 907 tests, 70 fichiers (Node, logique pure) |
| `npm run build` | succès |
| Corruption CSS | reproduite avant, verrouillée par test après |
| Essai dans l'app | **non fait** cette session |
