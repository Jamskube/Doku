# Journal — 2026-07-08

## Summary
Session fondatrice en deux temps. **Matin — M0 complet** : idée cadrée (brainstorm 19 idées), PRD v1 (12 FRs), spec UX validée (layout KUDE, sidebar repliée = invisible), architecture Tauri 2 + Svelte 5, 4 ADRs accepted, spike S0 mesuré : **CodeMirror 6 live preview choisi** (Milkdown rejeté : round-trip 0/8, 64× plus lent sur 500 Ko). Repo poussé sur GitHub (`Jamskube/Doku`). **Après-midi — W1 implémentée** : handoff Claude design importé, app réelle scaffoldée au root (Tauri 2 + Svelte 5 + Vite), shell W1 fidèle à la maquette (onglets Chrome-style, sidebar Obsidian repliable à 0 px, thème crème/sombre), éditeur live preview branché (checkboxes cliquables, wikilinks, mode source Ctrl+/), filet page/onglets retiré à la demande, icônes d'app générées depuis le logo Gemini, et **première fenêtre native Tauri validée par l'utilisateur**.

## Added
- `AGENTS.md`, `README.md`, structure `src/` + `docs/` — init projet
- `docs/planning/brainstorm-doku.md` — 19 idées priorisées (v1 / v1.5 / v2)
- `docs/planning/PRD.md` — 12 FRs (4 P0), NFRs chiffrés, phasage M0-M4
- `docs/planning/ux-spec.md` — layout à la KUDE + différence (sidebar repliée = 0 px), 5 wireframes, raccourcis validés
- `docs/planning/architecture.md` — Tauri 2 + Svelte 5, DocumentView par format, services TS
- `docs/planning/claude-design-prompt.md` — brief complet pour Claude design (tokens AIR inclus)
- `docs/adr/0001` stack Tauri+Svelte · `0002` moteur CM6 live preview · `0003` snapshots AppData · `0004` I/O plugins officiels — tous **accepted**
- `spike/` — prototypes A (Milkdown) / B (CM6 + plugin live-preview ~170 lignes), corpus 9 fichiers, protocole + résultats mesurés
- `src/assets/doku-mark-rounded.svg` — logo officiel (pli corrigé dans les maquettes : `currentColor` + fond token)
- **App réelle au root** : `package.json`, Vite/Svelte config, `index.html`, `public/` (favicon)
- `src/` — `App.svelte` (shell + raccourcis), `components/` (TitleBar onglets Chrome-style, Sidebar ruban+panneau, DocumentView), `lib/` (stores runes, tauri guard, demo), `lib/editor/` (thème design + live-preview porté du spike)
- `src-tauri/` — hôte Rust minimal (plugins fs/dialog/single-instance), capabilities explicites, icônes générées depuis l'image Gemini (crop auto PowerShell + `npx tauri icon`)
- `docs/design/w1/` — maquette officielle `.dc.html` + captures design et implémentation (light/dark)

## Discoveries
- Milkdown/remark-stringify réécrit **chaque** fichier au round-trip (tables re-paddées, `_it_`→`*it*`, échappements) — rédhibitoire pour éditer des fichiers existants
- CM6 : 500 Ko chargé en 27 ms vs 1 727 ms ; frappe 17 vs 69 ms/frame — le live preview est aussi le comportement exact spécifié par FR-3
- KUDE (repo de référence) : pièges documentés — I/O Rust custom à lignes numérotées, Google Fonts en @import, couche d'alias CSS legacy, thème par défaut incohérent
- CM6 ne rend que le viewport : les vérifications DOM (Playwright) doivent scroller la cible en vue ; un conteneur scrollable externe conserve son scrollTop entre `setState`

## Decisions
- Stack : Tauri 2 + Svelte 5 + TS (ADR-0001) · Moteur : CM6 live preview, mode source = même éditeur via Compartment (ADR-0002) · Snapshots : `%APPDATA%\Doku` centralisé (ADR-0003) · I/O : plugins officiels, zéro Rust custom (ADR-0004)
- UX : WYSIWYG à la Typora · sidebar repliée totalement invisible · mini-barre de sélection oui · compteur de mots non · thème crème par défaut

## Decisions (après-midi)
- Filet `--line-1` (haut de page + contour onglet actif) **retiré partout** à la demande de l'utilisateur — raccords courbes conservés sans trait (commit `c787bea`)
- Icône d'app = mark « D » blanc sur carré noir (image Gemini de l'utilisateur, croppée automatiquement)

## Commits
| Commit | Message |
|---|---|
| `de7a7e9` | chore: initial commit - planning, ADRs, spike S0 |
| `9e37be2` | docs: spike S0 verdict + adr-0002 cm6 |
| `e540f12` | feat: shell W1 + editeur live preview |
| `c787bea` | update: retire le filet page/onglets |
| `2e099f3` | chore: icones app + permissions fs natif |
