---
id: LES-20260720-001
status: active
severity: warning
scope: editor
tags: [editor]
applies_to: ["src/lib/editor/**"]
confidence: observed
created: 2026-07-20
last_verified: null
review_after: 2027-01-16
supersedes: []
source: ["AGENTS.md#memories-legacy (bloc migré)","docs/agent-knowledge/migrations/20260825-1815-legacy-memory.md"]
---
## Situation
Gotchas — portée `editor`. Entrée d'origine datée du 2026-07-20.

## Rule
Aperçu « en place » CM6 — (proposition rendue par-dessus une sélection, doc intact jusqu'à Accepter — `rephrase-preview.ts`, 16.2/w3) : (1) `Decoration.replace` inline **multi-lignes** = StateField obligatoire (jamais ViewP

## Evidence
lugin) ; (2) le widget doit **se re-peupler LUI-MÊME dans `toDOM()`** — la virtualisation CM6 détruit le DOM hors viewport et rappelle toDOM au retour, un conteneur rempli uniquement de l'extérieur revient VIDE (boutons morts) ; (3) auto-dismiss **atomique** dans `update()` sur tout `docChanged` sans effet marqueur — la transaction d'accept porte l'effet (changes + effet null ensemble), c'est ce qui la distingue d'une édition étrangère ; (4) re-valider `sliceDoc(from,to)===original` dans l'$effect de sync : un `setState` (rechargement externe, bump de `rev`) ne passe par AUCUNE transaction, ni le field ni l'updateListener ne le voient ; (5) boutons du widget : `mousedown`→`preventDefault()` seul, action sur `click` (sinon Entrée/Espace ne marchent pas) ; (6) deux replaces partiellement superposés (aperçu inline vs widget-bloc table) = territoire indéfini CM6 → faire sauter le widget-bloc tant que l'aperçu chevauche.
