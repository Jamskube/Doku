---
id: LES-20260713-005
status: active
severity: warning
scope: pdf
tags: [pdf]
applies_to: ["src/lib/pdf*/**","src/components/PdfView*"]
confidence: observed
created: 2026-07-13
last_verified: null
review_after: 2027-01-09
supersedes: []
source: ["AGENTS.md#memories-legacy (bloc migré)","docs/agent-knowledge/migrations/20260825-1815-legacy-memory.md"]
---
## Situation
Gotchas — portée `pdf`. Entrée d'origine datée du 2026-07-13.

## Rule
Ajouter un kind de document BINAIRE — (ex. PDF, `content=''`) : (1) brancher le kind **AVANT** toute lecture texte (`openPath`/`restoreSession`/`openFileDialog` → `openTab(name,path,'',kind)`), sinon `readTextFile`/`detectU

## Evidence
nsupported` rejette le binaire ; (2) **garder la garde de sauvegarde** en 1re ligne — aujourd'hui `snapshotTextTab` dans `saveTabOrSaveAs` (le délégué `saveTab` a été retiré le 2026-08-17, il n'avait plus d'appelant) — sans ça un **Ctrl+S écrit `content=''` sur le fichier et le DÉTRUIT** (le save n'est PAS gaté sur « modifié »). Vérifier que les autres chemins d'écriture (snapshot, reload externe, `invalidateSearchDoc`) sont inatteignables pour ce kind (un binaire `content===savedContent===''` n'est jamais `isDirty`)
