---
id: LES-20260714-002
status: active
severity: warning
scope: editor
tags: [editor]
applies_to: ["src/lib/editor/**"]
confidence: observed
created: 2026-07-14
last_verified: null
review_after: 2027-01-10
supersedes: []
source: ["AGENTS.md#memories-legacy (bloc migré)","docs/agent-knowledge/migrations/20260825-1815-legacy-memory.md"]
---
## Situation
Gotchas — portée `editor`. Entrée d'origine datée du 2026-07-14.

## Rule
Coller une image du presse-papier — (CM6 `EditorView.domEventHandlers({ paste })`) : extraire le `File` via `item.getAsFile()` **SYNCHRONEMENT** avant tout `await` — `event.clipboardData.items` devient inerte dès la fin du

## Evidence
dispatch synchrone (le `File`/`Blob` survit aux `await`, pas le `DataTransferItem`). Retourner **`true`** = pris en charge (CM ne colle pas de texte ; `preventDefault()` seul ne suffit PAS en CM6) ; **`false`** = laisser CM coller le texte normalement (pas d'image → fall-through). L'insertion `view.dispatch(state.replaceSelection('![](nom)'))` se fait dans une IIFE async après l'écriture fichier
