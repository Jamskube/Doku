---
id: LES-20260714-003
status: needs-review
severity: warning
scope: ia-locale
tags: [tauri, ollama]
applies_to: ["src-tauri/src/sidecar.rs","src/lib/ollama*"]
confidence: uncertain
created: 2026-07-14
last_verified: null
review_after: 2027-01-10
supersedes: []
source: ["AGENTS.md#memories-legacy (bloc migré)","docs/agent-knowledge/migrations/20260825-1815-legacy-memory.md"]
---
## Situation
Gotchas — portée `ia-locale`. Entrée d'origine datée du 2026-07-14.

## Rule
Sidecar Ollama sous Tauri 2 — (copilote IA local, ADR-0006/0012) : spawn `ollama serve` via `tauri-plugin-shell` (`app.shell().sidecar("ollama")`, permission `shell:allow-execute` scopée `{name, sidecar:true}`) ; **port éph

## Evidence
émère** (bind `127.0.0.1:0` en Rust → passer via `OLLAMA_HOST=127.0.0.1:<port>`) pour coexister avec un Ollama tiers ; **modèles isolés** `OLLAMA_MODELS=%APPDATA%\<app>\models` ; **CORS** `OLLAMA_ORIGINS=http://tauri.localhost` (origine WebView2 Windows) ; le front pilote par **`fetch` webview** vers `http://127.0.0.1:<port>` (pas besoin de plugin-http) → CSP `connect-src http://127.0.0.1:*` (port dynamique = wildcard loopback). Streaming = NDJSON (bufferiser les lignes partielles à travers les chunks) ; **annulation** = `AbortSignal` sur le `fetch` (coupe le flux client ET serveur — Ollama stoppe à la déconnexion). Octets/modèles restent locaux ; seul `/api/pull` sort (action explicite)
