# Autopilot — 2026-08-17

**Demande originale (verbatim)** : « maintenant ce que j'aimerais c'est que tu fasse un passage clean pour revoir l'app d'abord dans le backend , corriger ce qui peut l'être rendre plus léger ce qui peut l'être , enlever le code mort nettoyer optmiser etc ensuite tu passe au front end avec impeccable »

**Critère de fin** : backend nettoyé (code mort retiré, doublons factorisés, bundle allégé) PUIS frontend revu via `impeccable`, avec à chaque étape 714 tests verts minimum, `npm run check` à 0 erreur et build vert. PASS d'un critic indépendant contre la demande ci-dessus.

## Boucle
| # | État constaté | Chaîne (étape) | Commande | Résultat | Vérifié par |
|---|---|---|---|---|---|
| 0 | Arbre propre, HEAD `127d3fc` | — | `git status`, mesure du bundle | Découverte : `collaboration-worker-entry` = **4,6 Mo** livrés pour une collaboration jamais instanciée | `ls dist/assets` |
| 1 | Hypothèse des 4,6 Mo à vérifier | Reconnaissance | Instrumentation du constructeur `Worker` au banc | **RÉFUTÉE** : le worker `superdoc-v2-edit` créé EST ce fichier — le nom ment, c'est le moteur d'édition, pas la collaboration. Le retirer aurait cassé l'édition DOCX | Playwright, banc `docx-edit` |
| 1 | Poids réel inconnu | Reconnaissance | Analyse du graphe d'entrée de `dist/index.html` | **1 017 Ko au démarrage**, 30,4 Mo à la demande. Ni SuperDoc, ni MuPDF, ni pdf.js au boot. Les ~480 Ko tiers sont CodeMirror, cœur de l'éditeur → **rien à gagner côté bundle**, constat honnête plutôt que victoire inventée | mesure `dist/index.html` + `performance.getEntriesByType` |
| 1 | Hôte Rust non audité | Reconnaissance | Inventaire des 15 `#[tauri::command]` vs appels TS | **0 commande orpheline** — l'hôte Rust est propre | grep croisé |
| 1 | Pistes utilisateur à confirmer | Reconnaissance | Recherche croisée | `saveDocxDialogPath` : 1 seul appelant, interne au fichier → dé-exportable. `PdfParagraph.page` : **écrit une fois, jamais lu**. `undici` : vient de `jsdom`, dépendance de TEST, ne part jamais dans l'app | grep sur `src/`, `.agent/`, `scripts/` |

| 2 | Plan écrit, critique lancée | Medium : `/plan` → `/critic` → `/epct` (étape : exécution d'une étape indépendante pendant la critique) | `npm install dompurify@^3.4.13` | **`npm install` échouait sur le dépôt** (ERESOLVE : peer optionnel `pdfjs-dist@^5` de SuperDoc contre notre 6.2.108) — personne ne pouvait installer Doku depuis un clone neuf. Réparé par un `override` ciblé, puis correctif de sécurité DOMPurify appliqué | `npm install --dry-run` passe · 714 tests · check 0 · build vert · commit `5c3719b` |

| 3 | Plan critiqué : « ship with changes », 5 objections majeures | Medium (étape : `/epct`) | Correctifs Rust | `main.rs` : `OllamaState::new().expect(...)` faisait **paniquer Doku au démarrage** si le Job Object échouait — trouvaille de la critique, plus grave que mes 4 verrous. L'app démarre désormais, c'est le sidecar qui refuse, avec sa raison | `cargo check` · commit `f8d7090` |
| 4 | 3 fonctions de chemin fausses | Medium (étape : `/epct`) | Extraction de `src/lib/paths.ts` | La critique avait raison : une seule racine (`sepOf` mono-séparateur) causait les trois défauts. Corrigé là plutôt qu'aux feuilles. Tests prouvés MORDANTS (remis l'ancienne version : 2 échecs) | 724 tests · commit `8c662c6` |
| 5 | Code mort confirmé par la critique | Medium (étape : `/epct`) | Suppressions + AGENTS.md | `saveTab`, `toggleTheme`, `clearSearch` retirés ; `revealScope` CONVERGÉ (4 copies) au lieu d'être supprimé ; banc `pdf-docx` cassé retiré ; règle durable d'AGENTS.md:99 réécrite dans le même commit — elle nommait `saveTab` | 724 tests · commits `b219121`, `1ce953e` |

| 6 | Frontend : `PdfPagesDialog` restée en arrière | `impeccable polish` | Alignement D.A. | Contours permanents retirés, `--surface-hover`, échelle d'encre, **focus clavier ajouté** (il n'y en avait aucun), bloc mouvement réduit ajouté. Vérifié dans les deux thèmes : contraste 15,4:1 et 15,6:1 | Playwright, banc `pdf-pages` · commit `bdbc842` |
| 7 | **Critic d'achèvement : BLOCK** | Retour en boucle (§3.3) | Traitement des écarts | Verrous Rust : 16 `.lock().expect()` récupérés dans `openai.rs` + `compat.rs` — sans quoi une panique condamnait le bouton « Arrêter » du copilote jusqu'au redémarrage. Clients HTTP : 6 constructions par appel **sans délai d'expiration** → deux clients partagés, comme `compat.rs` le faisait déjà juste à côté | `cargo check` · commit `7282753` |
| 7 | **J'avais mesuré le mauvais axe du bundle** | Retour en boucle | Sous-ensembles de police | Le juge a raison : le poids qui compte est celui de l'INSTALLATEUR, pas du démarrage. 91 Ko de cyrillique, grec et vietnamien livrés dans une app française — alors que le projet applique DÉJÀ ce correctif à Source Serif. Inter et Geist Mono passés en latin seul | Polices : 716 → 625 Ko · rendu prouvé (618 px vs 591 px de repli) · commit `7282753` |
| 8 | 3 tests rouges par dépassement de délai | `/debug` | Délais explicites | Pas une régression : 11 s de calcul réel (pdf-lib + MuPDF WASM) contre un défaut de 5 s. Hypothèse « serveur de dev concurrent » **infirmée** par la mesure avant de conclure | 724 tests · commit `a67ce78` |
| 8 | Défaut visuel trouvé par le juge | `impeccable` | `WikilinkPrompt` | Le voile employait `rgba(var(--ink-rgb), 0.32)` : en thème sombre `--ink-rgb` vaut 235,237,241, donc **un lavis BLANC** au lieu d'assombrir. Leçon d'AGENTS.md du 2026-07-17 reproduite telle quelle. Voile noir + focus clavier + mouvement réduit | 724 tests · commit `b86f8fb` |

## Décisions autonomes
| Décision | Défaut choisi | Pourquoi | Réversible |
|---|---|---|---|
| Réparer `npm install` : `.npmrc legacy-peer-deps` ou `overrides` ? | `overrides: { "pdfjs-dist": "$pdfjs-dist" }` | Ciblé sur le seul conflit connu et documenté, au lieu de désactiver la vérification des peers pour TOUT le dépôt et de masquer les prochains conflits réels | Oui (3 lignes de `package.json`) |
| `stopOllama` + commande Rust `stop_ollama` | Retirer les deux | Preuve : `main.rs:82-90` arrête le sidecar via `state.shutdown()` sur `WindowEvent::Destroyed`, jamais via la commande. Aucun appelant TS ni Rust. Le Job Object reste le mécanisme porteur | Oui |
| `wikilink.ts` : converger vers `baseName` ? | **NON, laissé tel quel** | La critique a prouvé que ce n'est pas un doublon mais une garde anti-traversée : converger ferait passer `[[dossier/]]` de « refusé » à « crée `dossier.md` » — un fichier que l'app refuse aujourd'hui de créer | — |
| `isSupportedFile` : corriger l'extraction d'extension ? | **NON** | Contrainte utilisateur « aucun changement visible » : le correctif ferait passer la saisie `md` de « crée `md` » à « crée `md.md` ». Seul `kindFromName` est corrigé, où le défaut est dangereux (fichier nommé `pdf` traité comme binaire) | Oui |
| `parentFolder` : réécrire via `baseName(parentPath())` ? | **NON, laissé tel quel** | La critique a montré qu'il était déjà le seul helper correct sur les chemins mixtes. Ajouté 2 tests de non-régression au lieu de le toucher | — |

## Escalades (blocages travaillés)
_(aucune pour l'instant)_

## ⏸️ Parqué pour l'humain
_(vide)_

## Non tenté
_(vide)_
