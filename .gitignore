node_modules/
dist/
src-tauri/target/
src-tauri/gen/
*.local
.DS_Store
Thumbs.db
.playwright-mcp/
.impeccable/live/sessions/
# Binaire sidecar Ollama (exe + lib/ DLLs, ~50 Mo) — non commité, placé localement (voir src-tauri/binaries/README.md)
src-tauri/binaries/*
!src-tauri/binaries/README.md

# Journaux des bancs de contrôle (sorties de serveur de dev, jamais des preuves)
.agent/**/*.log

# Fixture locale du banc de modification de texte (document de l utilisateur)
.agent/visual/pdf-text-edit/doc.pdf

# Artefacts Linux téléchargés depuis la CI (jamais versionnés)
dist-linux/

# Paquet Arch téléchargé depuis la CI
dist-arch/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Source bgraph — confidentielle par licence (« The Source Is Confidential ») :
# reconstruire le WASM avec `npm run build:bgraph`, ne jamais publier le source.
/add-ons/

# Reglages Claude Code personnels a la machine
.claude/settings.local.json
